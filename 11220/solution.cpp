#include <iostream>
#include <string>
#include <vector>
using namespace std;
int main()
{
    int num;
    string cache;
    vector<vector<string>>data;
    while (cin >> num)
    {
        getline(cin,cache);
        for(int i = 0 ; i < num ; i++)
        {
            getline(cin,cache);
            if(cache == "")
                continue;
            
        }
    }
    
}